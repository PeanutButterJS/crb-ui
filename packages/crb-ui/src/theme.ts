export type Theme = {
  colors: {
    blue: string;
    white: string;
    lightGray: string;
  };
};

export const theme: Theme = {
  colors: {
    blue: '#0071B7',
    white: 'white',
    lightGray: '#EBF0F2'
  }
};
